[tool.ruff]
line-length = 140  # Увеличиваем лимит строки

[tool.ruff.lint]
extend-ignore = ["I001", "I002", "D100"]  # Гарантированно отключаем авторазбиение импортов
extend-select = ["I"]   # Включаем авто-сортировку импортов через линтер
